# ContractorAgent

Any change to a page or component follows `docs/ui-principles.md`: icons, colour and counts first, words
only when those cannot carry the meaning, and short lists with the rest one click away. Read it before
touching UI.
